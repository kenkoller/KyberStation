// Thin re-export — the implementation lives in the `layerstack/` subfolder
// (decomposed 2026-04-20, W6a). Existing imports of `{ LayerStack }` from
// this path continue to work; don't add new implementation here.
export { LayerStack } from './layerstack/LayerStack';
